apply plugin: "war"

bootWar {
    mainClassName = "com.ilqar.tst.ShowilqarApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
